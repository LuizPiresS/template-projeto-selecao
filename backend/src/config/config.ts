export const appConfig = {
  // app config
  port: 3003
}

export const dbConfig = {
  // database config
  db_type: 'mariadb',
  db_port: 3306,
  db_host: 'localhost',
  db_user: 'root',
  db_pass: 'root',
  db_name: 'magrathea'
}
